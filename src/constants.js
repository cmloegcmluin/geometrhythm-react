const WIDGET_DIAMETER = 400;
const CELL_ORIGIN_OFFSET = WIDGET_DIAMETER / (Math.sqrt(2) * 2);
const CELL_DIAMETER = 20;

/* eslint-disable import/prefer-default-export */
export {
  WIDGET_DIAMETER,
  CELL_DIAMETER,
  CELL_ORIGIN_OFFSET,
};
/* eslint-enable */
