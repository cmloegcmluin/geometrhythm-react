test("", () => {
})

// const mount = require('enzyme').mount;
// const AppPresenter = require('../../src/presenters/AppPresenter').default;
// const App = require('../../src/containers/App').default;
// const React = require('react');
// const Provider = require('react-redux').Provider;
// const configureStore = require('redux-mock-store').default;
// const ImmutableMap = require('immutable').Map;
//
// beforeEach(() => jest.resetModules());
//
// describe('app', () => {
//   test('connects an app presenter', () => {
//     const store = configureStore()(ImmutableMap());
//     const container = mount(<Provider {...{ store }}><App /></Provider>);
//     const presenter = container.find(AppPresenter);
//     expect(presenter.length).toBe(1);
//   });
// });
